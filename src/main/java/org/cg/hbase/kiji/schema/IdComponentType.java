package org.cg.hbase.kiji.schema;

/**
 * enum for id component type which is used for table scan
 * only support Long and Integer type for now
 * @author WZ
 *
 */
public enum IdComponentType {Long, Integer, String};
